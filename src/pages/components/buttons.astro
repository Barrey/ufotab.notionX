---
import DocsLayout from '../../layouts/DocsLayout.astro';
import PreviewWrapper from '../../components/PreviewWrapper.astro';
import { readFile } from 'node:fs/promises';

const button1 = await readFile(
  'src/components/html/buttons/button1.html',
  'utf-8'
);
const button2 = await readFile(
  'src/components/html/buttons/button2.html',
  'utf-8'
);
const button3 = await readFile(
  'src/components/html/buttons/button3.html',
  'utf-8'
);
const button4 = await readFile(
  'src/components/html/buttons/button4.html',
  'utf-8'
);
const button5 = await readFile(
  'src/components/html/buttons/button5.html',
  'utf-8'
);
const button6 = await readFile(
  'src/components/html/buttons/button6.html',
  'utf-8'
);
const button7 = await readFile(
  'src/components/html/buttons/button7.html',
  'utf-8'
);
---

<DocsLayout title="Buttons - Documentation">
  <div class="max-w-4xl">
    <p class="text-gray-600 mb-8">
      A collection of button components with different styles and variations.
    </p>

    <div class="space-y-12">
      <section>
        <p class="text-gray-600 mb-4">
          The button component is used for general actions and interactions.
        </p>
        <PreviewWrapper title="Button Example" code={button1} />
      </section>

      <section>
        <p class="text-gray-600 mb-4">
          The button component with color variants.
        </p>
        <PreviewWrapper title="Button with Icon Example" code={button2} />
      </section>

      <section>
        <p class="text-gray-600 mb-4">
          The button component with size variations.
        </p>
        <PreviewWrapper title="Button with size variations" code={button3} />
      </section>

      <section>
        <p class="text-gray-600 mb-4">The button component with states.</p>
        <PreviewWrapper title="Button with states" code={button4} />
      </section>

      <section>
        <p class="text-gray-600 mb-4">
          The button component with contextual usage.
        </p>
        <PreviewWrapper title="Button with contextual usage" code={button5} />
      </section>

      <section>
        <p class="text-gray-600 mb-4">The button component in group.</p>
        <PreviewWrapper title="Button in group" code={button6} />
      </section>

      <section>
        <p class="text-gray-600 mb-4">
          The button component with special effects.
        </p>
        <PreviewWrapper title="Button with special effects" code={button7} />
      </section>
    </div>
  </div>
</DocsLayout>
